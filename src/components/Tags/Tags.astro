---
const {tags} = Astro.props;

interface Props {
    tags: string[];
}
---

<div class="flex flex-wrap gap-2 mt-5">
    {tags.map((tag: string, index: number) => (
      <span class="border border-1 text-black p-1">
        <a href={'/tag/' + tag}>#{(tag)}</a>
      </span>
    ))}
  </div>